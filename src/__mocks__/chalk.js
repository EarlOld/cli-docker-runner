module.exports = {
  __esModule: true,
  default: (text) => text,
  red: (text) => text,
  green: (text) => text,
  yellow: (text) => text,
  blue: (text) => text,
  cyan: (text) => text,
  magenta: (text) => text,
  gray: (text) => text,
  bold: (text) => text,
  dim: (text) => text
};